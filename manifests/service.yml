apiVersion: v1
kind: Service
metadata:
    name: philstollerymodcloudnativeexercisecode
spec:
    type: LoadBalancer
    ports:
    - port: 80 
    selector:
        app: philstollerymodcloudnativeexercisecode